# OpenAI API Configuration
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Kestra Webhook Configuration
# For local development with ngrok: 
VITE_KESTRA_WEBHOOK_URL=https://your-ngrok-url.ngrok-free.app/api/v1/executions/webhook/contentflow/contentflow-handler/from-web?key=contentflow-key

# For production deployment using Netlify function proxy:
# VITE_KESTRA_WEBHOOK_URL=https://your-app-name.netlify.app/.netlify/functions/kestra-proxy/api/v1/executions/webhook/contentflow/contentflow-handler/contentflow-key

# Note: Copy this file to .env and fill in your actual values
# The .env file is gitignored for security
# Update the ngrok URL each time you restart ngrok for local development